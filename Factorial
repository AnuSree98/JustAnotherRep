// C Program to write factorial of a number using recursion.
#include <stdio.h>
int fact(n)
{
	if(n==0 || n==1)
	{
		return 1;
	}
	else
	{
		return n*fact(n-1);
	}
}
int main()
{
	int n,f;
	scanf("%d",&n);
	f=fact(n);
	printf("%d",f);
}
